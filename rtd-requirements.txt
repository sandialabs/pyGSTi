# Read-the-docs build requirements for pyGSTi.
# The pyGSTi package itself plus its [docs] optional-deps group (jupyter-book,
# jupytext, matplotlib). Single source of truth lives in pyproject.toml.
.[docs]
